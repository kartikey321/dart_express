name: session_security_example
description: Production-ready session security example with dart_express
version: 1.0.0
publish_to: none # Not for publishing, example only

environment:
  sdk: ^3.6.0

dependencies:
  dart_express:
    path: ../../../packages/dart_express

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
